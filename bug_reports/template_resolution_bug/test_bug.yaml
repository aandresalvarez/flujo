version: "0.1"
name: "template_bug_reproduction"

steps:
  - kind: step
    name: step1
    agent:
      id: "flujo.builtins.stringify"
    input: "Hello World"
    
  - kind: step
    name: step2
    agent:
      id: "flujo.builtins.stringify"
    input: "{{ steps.step1 }} - Processed"
